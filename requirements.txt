fastapi>=0.103.0
uvicorn>=0.23.2
sqlalchemy>=2.0.20
jinja2>=3.1.2
python-multipart>=0.0.6
packaging>=23.1
# Google OAuth (Authlib starlette client + httpx as the async HTTP backend)
authlib>=1.3.0
httpx>=0.25.0
# Starlette SessionMiddleware requires itsdangerous for signed cookies
itsdangerous>=2.1.2
