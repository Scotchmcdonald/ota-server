services:
  ota-server:
    build: .
    container_name: esp32-ota-server
    restart: unless-stopped
    environment:
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=admin
    ports:
      - "8321:8000"
    volumes:
      - ./data:/app/data